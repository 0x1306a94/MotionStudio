#pragma once

namespace motion {

// Editor live-preview chrome behind / around the composition.
enum class PreviewBackdrop {
    Black,        // Solid black (AE-like default).
    Transparent,  // Checkerboard indicating transparency.
};

}  // namespace motion
