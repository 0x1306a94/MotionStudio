---
description: Wait for user confirmation before committing spec or plan docs
alwaysApply: true
---

# spec / plan 提交

撰写或修订 `docs/superpowers/specs/`、`docs/superpowers/plans/`（及仅为此改的 `docs/README.md` 链接）时：

- 写完先给用户看
- **等用户明确确认后再 commit**
- 不要写完就自动提交
- 不要把同一份 spec/plan 的多次修订拆成一串小 commit
